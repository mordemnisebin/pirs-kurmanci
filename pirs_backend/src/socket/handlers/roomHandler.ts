import { Socket } from "socket.io";
// Burası ilerleyen sürümlerde gameSocket.ts dosyasının refaktörü için modüller barındıracak.
export const setupRoomHandlers = (socket: Socket) => {
    // Oda olayları
};
